node:
  id: "node-1"
  port: 8080

api_key: "dev-secret-key-2025"

cluster:
  name: "dev-local"
  lock:
    url: "https://server-lock.haas.software"

paths:
  state_file: "./data/state-node1.json"
  rootfs_base: "./data/rootfs-node1"
  artifacts: "./data/artifacts"
  cache: "./data/cache"
